// Input validation (DESIGN_SPEC.md §5). Implemented in Phase 1.

import type { DenseNetworkLayoutOptions } from "./types.js";

export function validateOptions(options: DenseNetworkLayoutOptions): void {
  throw new Error("nnet-svg: validateOptions is not implemented yet (Phase 1)");
}
